package application;

import java.util.Locale;
import java.util.Scanner;
import entities.AccountBank;

public class Program {

	public static void main(String[] args) {
		Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);
		AccountBank accInfo = new AccountBank()
		
		System.out.print("Enter account number: ");
		int accNumber = sc.nextInt();
		System.out.print("Enter account holder: ");
		String name = sc.nextLine();
		sc.nextLine();
		System.out.print("Is there an initial deposit (y/n)? ");
		char choice = sc.next().charAt(0);
		
		if(choice == 'y') {
			System.out.print("Enter initial deposit value: ");
			double balance = sc.nextDouble();
			AccountBank accountBank = new AccountBank(accNumber, name, balance);
		} else {
			AccountBank accountBank = new AccountBank(accNumber, name);
		}
		
		System.out.println("Account data:");
		System.out.println(AccountBank);
		
	}

}
